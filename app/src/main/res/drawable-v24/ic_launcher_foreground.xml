package edu.iastate.shoppinglist;

import androidx.appcompat.app.AppCompatActivity;
import androidx.lifecycle.Observer;
import androidx.lifecycle.ViewModelProvider;

import android.content.Intent;
import android.os.Bundle;
import android.text.Editable;
import android.text.TextWatcher;
import android.util.Log;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TableLayout;
import android.widget.TableRow;

import java.io.IOException;
import java.io.Serializable;

public class MainActivity extends AppCompatActivity
        implements Observer<ShoppingListModel>, Serializable {

    final String TAG = "MainActivity";

    private ShoppingListViewModel shoppingListViewModel;

    private String newListName;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        shoppingListViewModel = new ViewModelProvider(this,
                new ViewModelProvider.NewInstanceFactory()).get(ShoppingListViewModel.class);

        shoppingListViewModel.shoppingListModel.observe(this, this);



        if(getIntent().getSerializableExtra("returnFolder") != null) {
            shoppingListViewModel.shoppingListModel.setValue((ShoppingListModel) getIntent().getSerializableExtra("returnFolder"));
        }

        if(savedInstanceState == null){
            try{
                System.out.println(this.getApplicationContext().getFilesDir());
                shoppingListViewModel.load(this);
            } catch (IOException e){
                e.printStackTrace();
            } catch (ClassNotFoundException e){
                e.printStackTrace();
            }
        }

        EditText amountEdit = findViewById(R.id.new_list_name);
        amo